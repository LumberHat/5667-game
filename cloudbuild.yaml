options:
  logging: NONE

steps:
- name: 'build-image'
  script: |
    docker build -t us-west2-docker.pkg.dev/$PROJECT_ID/quickstart-docker-repo/quickstart-image .
  automapSubstitutions: true
images:
- 'us-west2-docker.pkg.dev/advanced-sof/quickstart-docker-repo/quickstart-image'